// Package domain contains pure business logic. Each subpackage represents
// one bounded concept (entity + service + ports). Domain code MUST NOT import
// from internal/adapter, internal/http, or internal/app.
package domain
