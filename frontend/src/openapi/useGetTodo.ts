/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import type {
  QueryKey,
  QueryClient,
  QueryObserverOptions,
  UseQueryResult
} from '@tanstack/react-query';
import { queryOptions, useQuery } from '@tanstack/react-query';
import fetch from '@/services/fetcher';
import type { Client, RequestConfig, ResponseErrorConfig } from '@/services/fetcher';
import type { GetTodoQueryResponse, GetTodoPathParams, GetTodo401, GetTodo404 } from './GetTodo.ts';

export const getTodoQueryKey = (id: GetTodoPathParams['id']) =>
  ['/v1/todos/:id', ...(id ? [id] : [])] as const;

export type GetTodoQueryKey = ReturnType<typeof getTodoQueryKey>;

/**
 * @summary Get todo by id
 * {@link /v1/todos/:id}
 */
export async function getTodo(
  id: GetTodoPathParams['id'],
  config: Partial<RequestConfig> & { client?: Client } = {}
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    GetTodoQueryResponse,
    ResponseErrorConfig<GetTodo401 | GetTodo404>,
    unknown
  >({ method: 'GET', url: `/v1/todos/${id}`, ...requestConfig });
  return res.data;
}

export function getTodoQueryOptions(
  id: GetTodoPathParams['id'],
  config: Partial<RequestConfig> & { client?: Client } = {}
) {
  const queryKey = getTodoQueryKey(id);
  return queryOptions<
    GetTodoQueryResponse,
    ResponseErrorConfig<GetTodo401 | GetTodo404>,
    GetTodoQueryResponse,
    typeof queryKey
  >({
    enabled: !!id,
    queryKey,
    queryFn: async ({ signal }) => {
      if (!config.signal) {
        config.signal = signal;
      }
      return getTodo(id, config);
    }
  });
}

/**
 * @summary Get todo by id
 * {@link /v1/todos/:id}
 */
export function useGetTodo<
  TData = GetTodoQueryResponse,
  TQueryData = GetTodoQueryResponse,
  TQueryKey extends QueryKey = GetTodoQueryKey
>(
  id: GetTodoPathParams['id'],
  options: {
    query?: Partial<
      QueryObserverOptions<
        GetTodoQueryResponse,
        ResponseErrorConfig<GetTodo401 | GetTodo404>,
        TData,
        TQueryData,
        TQueryKey
      >
    > & { client?: QueryClient };
    client?: Partial<RequestConfig> & { client?: Client };
  } = {}
) {
  const { query: queryConfig = {}, client: config = {} } = options ?? {};
  const { client: queryClient, ...queryOptions } = queryConfig;
  const queryKey = queryOptions?.queryKey ?? getTodoQueryKey(id);

  const query = useQuery(
    {
      ...getTodoQueryOptions(id, config),
      queryKey,
      ...queryOptions
    } as unknown as QueryObserverOptions,
    queryClient
  ) as UseQueryResult<TData, ResponseErrorConfig<GetTodo401 | GetTodo404>> & {
    queryKey: TQueryKey;
  };

  query.queryKey = queryKey as TQueryKey;

  return query;
}
