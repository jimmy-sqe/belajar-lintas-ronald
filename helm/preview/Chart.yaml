apiVersion: v2
name: preview
description: Ephemeral PR preview environment — frontend, backend, and optional services (postgresql, redis, mongodb, mysql, minio, rabbitmq)
type: application
version: 0.1.0
appVersion: "1.0.0"
