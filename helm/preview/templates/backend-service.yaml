apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: {{ include "preview.namespace" . }}
spec:
  selector:
    app: backend
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: {{ .Values.backend.port }}
