apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: {{ include "preview.namespace" . }}
spec:
  selector:
    app: frontend
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: {{ .Values.frontend.port }}
